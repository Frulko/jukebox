/**
 * One line per skin, and the line is the whole cost: delete it and the theme
 * — CSS, icon set, numbers — leaves the bundle. Import order is picker order.
 */
import './classic'
import './itunes12'
import './music'
import './spotify'
import './material'
import './studio'
