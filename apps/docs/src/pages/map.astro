---
// The map is a standalone document with its own styles: it is served as-is,
// read from the repository's docs/ folder at build time. No copy to keep in sync.
import map from '../../../../docs/map.html?raw'
---
<Fragment set:html={map} />
